
export interface TypeState {
    wordIndex: number;
    charIndex: number;
    lastChar: string;
}

export interface StatisticsData {
    correctChars: number;
    correctWords: number;
    incorrectWords: number;
}